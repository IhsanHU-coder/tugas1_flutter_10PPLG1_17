import 'package:flutter/widgets.dart';

class ProfileFragments extends StatelessWidget {
  const ProfileFragments({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("ini Profile"),);
  }
}